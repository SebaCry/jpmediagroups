---
/**
 * Contacto — /es/contacto/.
 *
 * Misma plantilla que /contact/. Las etiquetas, los mensajes de validación y
 * las preguntas frecuentes están en español; los atributos `name` de los campos
 * NO se traducen, porque son el contrato con la plantilla de EmailJS y
 * renombrarlos hace que el correo llegue en blanco sin dar ningún error.
 */
import ContactPage from "../../components/ContactPage.astro";
---

<ContactPage lang="es" />
