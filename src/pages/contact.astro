---
/**
 * Contact, in English — /contact/.
 *
 * The page itself is `components/ContactPage.astro`, shared with the Spanish
 * route at /es/contacto/. The form is the one piece of real machinery on the
 * site — validation, an EmailJS send, a status region — and duplicating it per
 * language is how one of the two copies quietly stops working.
 */
import ContactPage from "../components/ContactPage.astro";
---

<ContactPage lang="en" />
